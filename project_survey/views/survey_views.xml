<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Configure survey at Stage -->
    <record id="survey_user_input_form_project_survey" model="ir.ui.view">
        <field name="name">survey.user_input.form.project.survey</field>
        <field name="model">survey.user_input</field>
        <field name="inherit_id" ref="survey.survey_user_input_view_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='state']" position="before">
                <field name="task_id" optional="hide"/>
            </xpath>
        </field>
    </record>

    <record id="survey_question_form_project_survey" model="ir.ui.view">
        <field name="name">survey.question.form.project.survey</field>
        <field name="model">survey.question</field>
        <field name="inherit_id" ref="survey.survey_question_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='suggested_answer_ids']/list/field[@name='answer_score']" position="after">
                <field name="activity_type_id"/>
            </xpath>
        </field>
    </record>
    
</odoo>
